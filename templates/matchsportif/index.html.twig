{% extends 'base.html.twig' %}

{% block title %}Matchsportif index{% endblock %}

{% block body %}
    <h1>Matchsportif index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Lieu</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for matchsportif in matchsportifs %}
            <tr>
                <td>{{ matchsportif.id }}</td>
                <td>{{ matchsportif.date ? matchsportif.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ matchsportif.lieu }}</td>
                <td>
                    <a href="{{ path('app_matchsportif_show', {'id': matchsportif.id}) }}">show</a>
                    <a href="{{ path('app_matchsportif_edit', {'id': matchsportif.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_matchsportif_new') }}">Create new</a>
{% endblock %}
