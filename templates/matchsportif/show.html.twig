{% extends 'base.html.twig' %}

{% block title %}Matchsportif{% endblock %}

{% block body %}
    <h1>Matchsportif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matchsportif.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ matchsportif.date ? matchsportif.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ matchsportif.lieu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_matchsportif_index') }}">back to list</a>

    <a href="{{ path('app_matchsportif_edit', {'id': matchsportif.id}) }}">edit</a>

    {{ include('matchsportif/_delete_form.html.twig') }}
{% endblock %}
