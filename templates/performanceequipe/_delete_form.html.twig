<form method="post" action="{{ path('app_performanceequipe_delete', {'id': performanceequipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ performanceequipe.id) }}">
    <button class="btn">Delete</button>
</form>
