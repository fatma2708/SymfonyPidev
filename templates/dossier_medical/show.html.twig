{% extends 'base.html.twig' %}

{% block title %}Détails du Dossier Médical{% endblock %}

{% block stylesheets %}
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome 6 -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Custom CSS -->
    <style>
        .sidebar {
            height: 100vh;
            position: fixed;
            top: 0;
            left: 0;
            width: 280px;
            transition: all 0.3s;
            z-index: 1000;
            background: #ffffff;
            box-shadow: 0 0 35px 0 rgba(49,57,66,.1);
        }

        .main-content {
            margin-left: 280px;
            padding: 20px;
            transition: all 0.3s;
        }

        @media (max-width: 768px) {
            .sidebar {
                margin-left: -280px;
            }
            .main-content {
                margin-left: 0;
            }
            .sidebar.active {
                margin-left: 0;
            }
        }

        .navbar {
            padding: 15px 20px;
            background: #fff !important;
            box-shadow: 0 1px 15px rgba(0,0,0,.04);
        }

        .card {
            border: none;
            box-shadow: 0 0 35px 0 rgba(49,57,66,.1);
            border-radius: 12px;
        }

        .btn-primary {
            transition: transform 0.2s ease-in-out;
        }

        .btn-primary:hover {
            transform: scale(1.05);
        }
    </style>
{% endblock %}

{% block body %}
<div class="wrapper">
    <!-- Sidebar -->
    <nav class="sidebar bg-white">
        <div class="sidebar-header p-4">
            <h3 class="fw-bold text-primary">Medical Manager</h3>
        </div>
        <div class="sidebar-menu p-3">
            <ul class="nav flex-column">
                <li class="nav-item mb-2">
                    <a href="{{ path('app_dossier_medical_index') }}" class="nav-link active bg-light rounded">
                        <i class="fas fa-notes-medical me-3"></i> Dossiers Médicaux
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a href="{{ path('app_blessure_index') }}" class="nav-link">
                        <i class="fas fa-user-injured me-3"></i> Gestion des Blessures
                    </a>
                </li>
                <li class="nav-item mb-2">
                    <a href="#" class="nav-link">
                        <i class="fas fa-cog me-3"></i> Settings
                    </a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="main-content">
        <!-- Header -->
        <nav class="navbar navbar-expand-lg">
            <div class="container-fluid">
                <button class="btn btn-link d-lg-none" type="button" id="sidebarToggle">
                    <i class="fas fa-bars"></i>
                </button>
                <div class="d-flex align-items-center ms-auto">
                    <div class="dropdown">
                        <a class="btn btn-link" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user-circle fa-xl"></i>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="#">Profile</a></li>
                            <li><a class="dropdown-item" href="#">Settings</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="#">Logout</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </nav>

        <!-- Main Content -->
        <div class="container-fluid">
            <div class="row mb-4">
                <div class="col">
                    <h2 class="fw-bold">
                        <i class="fas fa-file-medical-alt me-2"></i>Détails du Dossier Médical
                    </h2>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="{{ path('app_dossier_medical_index') }}">Dossiers Médicaux</a></li>
                            <li class="breadcrumb-item active" aria-current="page">Détails</li>
                        </ol>
                    </nav>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <table class="table table-borderless">
                        <tbody>
                            <tr>
                                <th scope="row"><i class="fas fa-user-md me-2 text-muted"></i>Athlète</th>
                                <td>{{ dossier_medical.athlete.name }}</td>
                            </tr>
                            <tr>
                                <th scope="row"><i class="fas fa-allergies me-2 text-muted"></i>Allergies</th>
                                <td>{{ dossier_medical.allergies }}</td>
                            </tr>
                            <tr>
                                <th scope="row"><i class="fas fa-calendar-check me-2 text-muted"></i>Dernier Check-up</th>
                                <td>{{ dossier_medical.dernierCheckup ? dossier_medical.dernierCheckup|date('d/m/Y H:i') : '-' }}</td>
                            </tr>
                            <tr>
                                <th scope="row"><i class="fas fa-file-alt me-2 text-muted"></i>Description</th>
                                <td>{{ dossier_medical.description }}</td>
                            </tr>
                            <tr>
                                <th scope="row"><i class="fas fa-heartbeat me-2 text-muted"></i>État de l'Athlète</th>
                                <td>
                                    <span class="badge 
                                        {% if dossier_medical.etatAthlete == 'Sain' %}bg-success
                                        {% elseif dossier_medical.etatAthlete == 'Blessé' %}bg-danger
                                        {% else %}bg-secondary{% endif %}">
                                        {{ dossier_medical.etatAthlete }}
                                    </span>
                                </td>
                            </tr>
                            <tr>
                                <th scope="row"><i class="fas fa-syringe me-2 text-muted"></i>Vaccinations</th>
                                <td>{{ dossier_medical.vaccinations }}</td>
                            </tr>
                        </tbody>
                    </table>

                    <h3 class="mt-4 mb-3">
                        <i class="fas fa-user-injured me-2"></i>Blessures Associées
                    </h3>

                    {% if blessures is not empty %}
                        <ul class="list-group">
                            {% for blessure in blessures %}
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    <span>
                                        <strong>Type :</strong> {{ blessure.type }} | 
                                        <strong>Date :</strong> {{ blessure.dateBlessure|date('d/m/Y') }}
                                    </span>
                                    <span class="badge 
                                        {% if blessure.type == 'Légère' %}bg-info text-dark
                                        {% elseif blessure.type == 'Grave' %}bg-danger
                                        {% else %}bg-secondary{% endif %}">
                                        {{ blessure.type }}
                                    </span>
                                </li>
                            {% endfor %}
                        </ul>
                    {% else %}
                        <div class="alert alert-warning" role="alert">
                            <i class="fas fa-exclamation-circle me-2"></i>Aucune blessure associée à cet athlète.
                        </div>
                    {% endif %}

                    <div class="d-flex gap-2 mt-4">
                    <a href="{{ path('dossier_medical_export_pdf', {'id': dossier_medical.id}) }}" class="btn btn-warning">
                            <i class="fas fa-edit me-2"></i>Exporter PDF
                        </a>
                        <a href="{{ path('app_dossier_medical_edit', {'id': dossier_medical.id}) }}" class="btn btn-warning">
                            <i class="fas fa-edit me-2"></i>Modifier
                        </a>
                        <a href="{{ path('app_dossier_medical_index') }}" class="btn btn-secondary">
                            <i class="fas fa-arrow-left me-2"></i>Retour à la liste
                        </a>
                    </div>

                    <!-- Formulaire de suppression -->
                    <div class="mt-4">
                        {{ include('dossier_medical/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        $(document).ready(function() {
            // Toggle sidebar on mobile
            $('#sidebarToggle').click(function() {
                $('.sidebar').toggleClass('active');
                $('.main-content').toggleClass('active');
            });

            // Tooltips
            const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
            tooltipTriggerList.forEach(el => new bootstrap.Tooltip(el));
        });
    </script>
{% endblock %}