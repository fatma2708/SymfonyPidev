<form method="post" action="{{ path('app_performanceathlete_delete', {'id': performanceathlete.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ performanceathlete.id) }}">
    <button class="btn">Delete</button>
</form>
