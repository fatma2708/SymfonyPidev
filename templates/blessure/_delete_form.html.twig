<form method="post" action="{{ path('app_blessure_delete', {'id': blessure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blessure.id) }}">
    <button class="btn">Delete</button>
</form>
